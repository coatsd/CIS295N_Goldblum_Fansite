@model List<Book>

@{
    ViewData["Title"] = "Book Sources";
    ViewData["Comment"] = "This page will contain book information that talks about Jeff Goldblum.";
    Layout = "_TableLayout";
}

<table>
    <tr>
        <th>Book</th>
        <th>Link</th>
    </tr>
    @for (int i = 0; i < Model.Count; i++)
    {
        <tr>
            <td>
                @Model[i].Title <br />
                @Model[i].Author <br />
                @Model[i].Genre <br />
                @Model[i].Publisher <br />
                @Model[i].PubDate
            </td>
        <td>
            <a href="@Model[i].Link">@Model[i].Link</a>
        </td>
    </tr>
}
</table>